export type StoreType = {
  cats: [];
  categoria: [];
  limit: number;
  id: number;
};

export type Categories = {
  id: number;
  name: string;
};

export type CatsType = {
  id: string;
  url: string;
  width: number;
  height: number;
};
